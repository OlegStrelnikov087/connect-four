export enum GameStatus {
    Over = 'over',
    Pending = 'pending',
    Waiting = 'waiting'
}

export enum ChipValues {
    Player1 = 1,
    Player2 = 2
}

export enum PlayerTypes {
    User = 'user',
    Bot = 'bot'
}