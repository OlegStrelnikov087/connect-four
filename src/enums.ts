export enum GameStatus {
    Over = 'over',
    Pending = 'pending',
    Waiting = 'waiting'
}

export enum PlayerTypes {
    User = 'user',
    Bot = 'bot'
}

export enum CellValue {
    Player1 = 1,
    Player2 = 2,
    EmpryCell = 0
}

export enum ChipColors {
    Red = 'red',
    Yellow = 'yellow'
}